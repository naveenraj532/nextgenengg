Options -MultiViews
RewriteEngine On
RewriteBase /

# Let existing files and directories load normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send every other request to the React app entrypoint.
RewriteRule ^ index.html [L]
